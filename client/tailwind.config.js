const config = {
    theme: {
        extend: {
            fontFamily: {
                sans: ["var(--font-sans)", "sans-serif"], // corpo
                serif: ["var(--font-serif)", "serif"], // títulos
            },
        },
    },
};

export default config;
